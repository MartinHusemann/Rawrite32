To make this source compile you need

 - MS DevStudio .NET
 - some additional files

Copy the following files from the NetBSD distribution into the NetBSD 
subdirectory:

src/lib/libc/md/md5c.c
src/sys/sys/md5.h

Get the complete zlib distribution and unpack it in the zlib subdirectory.
